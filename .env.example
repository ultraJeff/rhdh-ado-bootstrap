# =============================================================================
# RHDH Bootstrap Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# The .env file is gitignored and will not be committed.
# =============================================================================

# -----------------------------------------------------------------------------
# Cluster Configuration
# -----------------------------------------------------------------------------
# The base domain for your OpenShift cluster
# Example: apps.cluster-abc123.sandbox.opentlc.com
CLUSTER_DOMAIN=apps.your-cluster.example.com

# Keycloak/SSO hostname (typically sso.${CLUSTER_DOMAIN})
KEYCLOAK_HOSTNAME=sso.apps.your-cluster.example.com

# Dev Spaces URL (typically devspaces.${CLUSTER_DOMAIN})
DEVSPACES_URL=https://devspaces.apps.your-cluster.example.com

# -----------------------------------------------------------------------------
# GitHub Configuration
# -----------------------------------------------------------------------------
# Your GitHub username or organization name
GITHUB_ORG=your-github-org

# Personal Access Token for GitHub API (avoids rate limiting)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, read:org
GITHUB_TOKEN=ghp_xxxxxxxxxxxx

# -----------------------------------------------------------------------------
# Azure DevOps Configuration  
# -----------------------------------------------------------------------------
# Your Azure DevOps organization name
AZURE_DEVOPS_ORG=your-azure-org

# Azure DevOps project name
AZURE_DEVOPS_PROJECT=your-project

# Azure DevOps Personal Access Token
# Create at: https://dev.azure.com/{org}/_usersSettings/tokens
# Required scopes: Code (Read & Write), Build (Read & Execute)
AZURE_TOKEN=your-azure-pat

# -----------------------------------------------------------------------------
# To apply these values:
# -----------------------------------------------------------------------------
# 1. Update keycloak/keycloak-instance.yaml with KEYCLOAK_HOSTNAME
# 2. Update secrets files with base64-encoded values
# 3. When using the scaffolder template, enter DEVSPACES_URL in the form
# =============================================================================
